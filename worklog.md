# worklog

## 2026-09-13 — 저장 목록을 탭으로, 파일 내보내기/가져오기
- **요청**: "코드를 저장 기능이 있으면 좋겠어.. 나중에 읽어 볼 수 있게." → "저장하고.. 상단에 저장된 목록을 보고 싶어."
- **결과**:
  - **상단 탭** — 드롭다운(`<select>`)을 꺼내 항상 보이는 탭 줄로 바꿨다. 탭 클릭=이동, 더블클릭=이름, `+`=새로, `✕`=삭제(**지금 보고 있는 탭에만** — 모든 탭에 있으면 옮기려다 지운다).
  - **저장 시각** — "저장됨 · 3분 전". **내용이 바뀔 때만** 찍는다(저장마다 찍으면 탭만 옮겨도 "방금"이 떠서 쓸모가 없어진다).
  - **내보내기 `.py` / 전체 백업 `.json` / 가져오기** + 에디터에 끌어다 놓기. localStorage 는 이 브라우저에만 있고 사파리는 ITP 로 스스로 지우기도 해서, 오래 둘 코드엔 브라우저 밖 복사본이 필요하다.
  - **가져오기는 덮어쓰지 않고 뒤에 붙인다** — 백업을 잘못 골라도 쓰던 코드가 사라지면 안 된다. 이름 충돌은 `(2)`.
  - 내용이 있는 연습장 삭제에 확인창 추가(전에는 즉시 삭제였다).
- **검증**: 브라우저 실측 — 가져오기 5종(정상 .py / 이름충돌 / 백업 .json 2개 / 깨진 json / 슬롯없는 json)에서 깨진 입력이 기존 슬롯을 건드리지 않음 확인. 탭 왕복 후 저장 시각 불변, 빈 슬롯 삭제 후 활성 인덱스 유효, 줄번호·색칠·도움말 정상.

## 2026-09-13 — 도움말 창 + 줄 조작 단축키
- **요청**: "혹시 추가적으로 필요한 기능이 있을려나? 그리고 short cut 모르니깐 상단에 help 클릭하면 나오는 화면도 하나 만들어줘"
- **결과**:
  - **도움말** — 상단 `도움말` 버튼 / <kbd>⌘?</kbd>. 네이티브 `<dialog>` 라 Esc·포커스 가둠·배경이 공짜. 키 이름은 플랫폼 보고 채운다(맥 `⌘⌥`, 그 외 `Ctrl`/`Alt`).
  - **줄 조작** — <kbd>⌘D</kbd> 복제, <kbd>⌘⇧K</kbd> 삭제, <kbd>⌥↑↓</kbd> 이동, <kbd>⌘S</kbd> 저장(브라우저 저장창을 가로채고 "저장됨" 표시).
  - 선택 범위 계산을 `lineSpan()` 으로 모았다 — 개행에 딱 걸친 선택이 다음 줄까지 먹던 계산을 네 군데서 따로 하고 있었다.
- **검증**: 브라우저 실측 — ⌥↓ 후 ⌥↑ 가 원본과 바이트 일치, 마지막 줄 삭제에 빈 줄 안 남음, ⌘S/⌘? `preventDefault` 확인, 글리프(⌘⇧⌥↑↓) 렌더 확인.

## 2026-09-13 — 줄 번호 · 주석 토글 · ⌘R 실행
- **요청**: "주석기능하고 라인넘버 넣어줘.. cmd + r 실행 바로가기도"
- **결과**:
  - **줄 번호**: 세 번째 `.layer`. 너비는 자릿수에 따라 JS 가 `--gutter` 로 정하고(고정폭이면 1000줄에서 잘림), 줄 수가 바뀔 때만 다시 그린다. **세로만 동기화하고 가로는 고정** — 그래서 배경이 불투명해야 한다.
  - **주석 토글 ⌘/Ctrl + /**: 전부 주석이면 풀고 아니면 전부 건다(반반일 때 줄마다 뒤집으면 왕복이 안 맞는다). `#` 을 가장 얕은 들여쓰기에 세로로 정렬.
  - **⌘/Ctrl + R 실행**: 에디터 포커스일 때만 새로고침을 가로챈다.
- **검증**: 브라우저 실측 — 121줄에서 거터 `3ch` 자동 확장, 가로 스크롤 600일 때 거터 `scrollLeft=0` 유지, 주석 왕복이 원본과 바이트 일치, `preventDefault` 확인.
- **메모**: Chrome 자동화 도구의 `cmd+/`·`cmd+r` 키 액션은 **페이지에 전달되지 않는다**(도구가 먼저 먹는다). 합성 `KeyboardEvent` 로 검증했다.

## 2026-09-13 — 파이썬 문법 색칠
- **요청**: "coloring 도 좀 해줘."
- **결과**: `dist/index.html` — textarea 뒤에 색칠한 `<pre>` 를 깔고 앞 글자만 투명하게(CodeMirror 없이 의존성 0). 대안 순서를 주석→문자열→키워드→숫자→내장 으로 고정해 `# def` 와 문자열 안의 단어가 안 물들게 했고, 삼중 따옴표를 홑따옴표보다 앞에 뒀다. 두 겹 정렬은 `.layer` 한 클래스로 묶고 스크롤 동기화. 브라우저에서 주석/독스트링/실행/자동 인덴트 전부 확인. 배포 완료.

## 2026-09-13 — 자동 인덴트
- **요청**: "그리고.. 웹사이트에 자동 인덴트 정도는 해줘야지."
- **결과**: Tab/Shift+Tab(블록 단위), Enter(들여쓰기 유지 · `:` 뒤 +4 · return/pass/break/continue/raise 뒤 −4), Backspace(4칸 단위), ⌘/Ctrl+Enter 실행. `document.execCommand('insertText')` 로 브라우저 실행취소 스택 보존.

## 2026-09-13 — note.daiyongkim.com 파이썬 연습장
- **요청**: "note.daiyongkim.com 에.. python 코딩 테스트 할 수 있는 걸 만들고 싶어" → "그냥 따라하기 practice 같은거야.. 빌드 gradle 설치하는건 번거러워서.." → "너가 cloudflare 들어가서 해줘."
- **결과**: Pyodide(CPython→WASM) 기반 브라우저 실행 연습장. 서버·샌드박스 불필요. GitHub Pages 배포(`dist/`, 빌드 단계 없음), Cloudflare DNS + 커스텀 도메인 + HTTPS 강제까지 완료. 
  - 함정: **워크플로 방식 Pages 배포는 `CNAME` 파일만으로 커스텀 도메인이 안 잡힌다** — `gh api -X PUT .../pages -f cname=...` 로 따로 넣어야 했다. `https_enforced` 를 같이 주면 인증서 발급 전이라 404 로 거부되므로 도메인 → 인증서 대기 → HTTPS 강제 2단계.
